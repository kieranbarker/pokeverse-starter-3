/* eslint-disable no-unused-vars */

import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { expect, test } from "vitest";
import App from "../src/components/App.jsx";

test("The original 151 Pokémon are rendered", { skip: true }, async () => {
  // your test here
});

test("Pokémon can be added to the party", { skip: true }, async () => {
  // your test here
});

test("Pokémon can be removed from the party", { skip: true }, async () => {
  // your test here
});

test(
  "Can add a maximum of six Pokémon to the party",
  { skip: true },
  async () => {
    // your test here
  }
);

test(
  "Can battle when there are at least two Pokémon in the party",
  { skip: true },
  async () => {
    // your test here
  }
);
